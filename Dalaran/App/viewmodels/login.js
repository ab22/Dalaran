define(['plugins/http', 'knockout', 'jquery'],
    function (http, ko, $) {
        var self;
        var Login = funtion()
        {
            self = this;
        };

        return Login;

    }
);